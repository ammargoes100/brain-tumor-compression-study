# Models

This folder is intentionally kept light. Trained PyTorch checkpoints are generated during notebook execution and saved to `/kaggle/working`.

Avoid committing large `.pth` files directly to GitHub unless your assignment specifically requires them.
